# Python packages installed into the Airflow containers
# Airflow tasks run Python directly, so these must be available inside Docker

# HTTP calls to the ERP API
requests==2.31.0

# PostgreSQL driver (used by extractor and dlt)
psycopg2-binary==2.9.9

# dlt — moves data from lake to warehouse
dlt[postgres]==0.4.12

# dbt — transforms data inside the warehouse
dbt-core==1.7.17
dbt-postgres==1.7.17

# Environment variable loading
python-dotenv==1.0.0